#include <vector>
#include <iostream>
#include <list>


int main()
{
    /*
        assign
        front
        back
        begin
        end
        clear

        emplace
        emplace back
        emplace front

        empty
        erase
        insert
        merge
        pop back
        pop front
        push back
        push front

        remove
        removeif

        size
        sort
        splice
        reverse
        swap
        unique
    */
    std::list<int> list(10);

    list.
}